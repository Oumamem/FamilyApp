{% extends 'base.html.twig' %}

{% block title %}Souvenir{% endblock %}

{% block body %}
    <h1>Souvenir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ souvenir.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ souvenir.date ? souvenir.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ souvenir.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ souvenir.description }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ souvenir.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('souvenir_index') }}">back to list</a>

    <a href="{{ path('souvenir_edit', {'id': souvenir.id}) }}">edit</a>

    {{ include('souvenir/_delete_form.html.twig') }}
{% endblock %}
